# Backend Helm Chart

This chart deploys the Go backend application together with a PostgreSQL database.

## Prerequisites
- Kubernetes cluster
- [Helm](https://helm.sh/) v3

## Installing the Chart
```bash
helm install wikidocify charts/backend
```

## Configuration
See `values.yaml` for the list of configurable parameters.
